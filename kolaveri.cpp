#include<iostream>
#include<stdio.h>
using namespace std;

int main(){
	int tests;
	scanf("%d",&tests);
	while(tests--){
		char ar[10000000];
	/*	int n,num;
		scanf("%d",&n);
		num=n;
		int arr[100001]={0};
		if(n==1)
			printf("1\n");
		else{
			int skip = 1;
			arr[1]=1;
			n--;
			for(int i = 2 ; n>2 ; ){
				int count =0,j;
				for(int j=i ; count<skip ; );
				{
				//	if(arr[j]==0){
						count++;
					}			
						j=(j+1)%(num+1);
						if(j==0)
							j++;				
				}
				for(int k=j ; arr[k]==1 ; ){
					k=(k+1)%(num+1);
					if(k==0)
						k++;
				}
				arr[k]=1;
				n--;
				skip++;
				i=(k+1)%(num+1);
				if(i==0)
					i++;
			}
			if()
				
			
		}
	}*/
}}

