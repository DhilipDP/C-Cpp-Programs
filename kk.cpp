#include<iostream>
#include<stdio.h>

using namespace std;

int 
int main(){
	while(1){
		int n1,n2;
		int i,fs,fe,ss,se,j;
		scanf("%d",&n1);
		int first[n1];
		for(i=0 ; i<n1 ; i++)
			scanf("%d",&first[i]);
		scanf("%d",&n2);
		for(i=0 ; i<n2 ; i++)
			scanf("%d",&second[i]);
		long int sum = 0;
		for(i=0 ,j=0; i<n1 || j<n2 ; i++,j=0){
			if(first[i]==second[i]){
				find_sum(first,second,fs,)
			}
		}
		
			
	}
}
